<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>CourseSphere</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.4.1/dist/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    <link rel="stylesheet" href="main.css">
</head>
<style>
@media screen and (max-width: 390px) {
  body {
    font-size: 16px;
  }

  .container {
    width: 95%;
    padding: 10px;
  }

  header {
    flex-direction: column;
    align-items: center;
  }

  nav ul {
    flex-direction: column;
    align-items: center;
  }

  nav li {
    margin: 5px 0;
  }

  .hero h1 {
    font-size: 2rem;
  }

  .hero p {
    font-size: 1rem;
  }

  .cta-button {
    font-size: 1rem;
    padding: 10px 20px;
  }

  .features {
    flex-direction: column;
  }

  .feature {
    width: 100%;
    margin-bottom: 20px;
  }

  footer {
    flex-direction: column;
    align-items: center;
    text-align: center;
  }

  .footer-links, .social-media {
    margin-top: 10px;
  }
}
</style>

<body>
    {{#if user}}
    <header>
        <h6>Welcome, {{user}}!</h6>
        <a href="/logout">Logout</a>
    </header>
    {{else}}
    <header>
        <div class="title">CourseSphere</div>
        <div class="auth-buttons">
            <a class="register-btn" href="/register">Register</a>
            <a class="login-btn" href="/login">Login</a>
        </div>
    </header>
    {{/if}}
    
    {{#if error}}
    <div class="error-box">
        {{error}}
    </div>
    {{/if}}

    {{{body}}}

    <footer style="position: fixed; bottom: 0; width: 100%;">
        <p>© 2024 CourseSphere, Created by Kumar Harsh, Ansh Gopinath, and Amal Shifwath Shaik.</p>
    </footer>
</body>
</html>